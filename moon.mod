name = "moonbit-community/asm"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "x86-64", "jit", "assembler", "native" ]

description = "A native-only x86-64 code emitter for MoonBit."

preferred_target = "native"
